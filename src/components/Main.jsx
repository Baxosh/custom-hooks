export default function Main() {
    // const response = useFetch({url: 'todos'})

    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
            {/*{result.length && (*/}
            {/*    result.map(item => (*/}
            {/*        <h1 key={item.id}>*/}
            {/*            {item.title}&nbsp; &nbsp;*/}
            {/*            <b>{item.description_for_todo}</b>*/}
            {/*        </h1>*/}
            {/*    ))*/}
            {/*)}*/}
        </div>
    )
}
